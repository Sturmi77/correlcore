# Copy to apps/android/android/keystore.properties (gitignored) for local release builds.
# Prefer CI secrets (ANDROID_KEYSTORE_BASE64 + passwords) for GitHub Actions.
storeFile=/absolute/path/to/correlcore-upload.keystore
storePassword=change-me
keyAlias=correlcore
keyPassword=change-me
