import { defaultDevPhaseFixture } from '$lib/dev/phaseFixtures';

export const mockEntries = defaultDevPhaseFixture.entries;
export const mockDashboardSummary = defaultDevPhaseFixture.dashboard;
export const mockUserPreferences = defaultDevPhaseFixture.preferences;
