# ============================================================================
# GENERATED FILE — DO NOT EDIT.
# Generated from infra/docker/docker-compose.quickstart.yml by
# scripts/gen_compose_stacks.py (#781). Edit the canonical quickstart stack or
# that script and run `python scripts/gen_compose_stacks.py`, then commit.
# See docs/selfhost/COMPOSE_STACKS.md.
# ============================================================================
#
# CorrelCore — Dockge drop-in stack.
# Copy compose.yaml + .env to /opt/stacks/correlcore/ and Deploy in the
# Dockge UI. Tailscale-internal homelab (no Traefik/TLS); ports bind to
# ${TAILSCALE_IP} (default 127.0.0.1). GlitchTip is omitted here; use the
# user-test or quickstart stack (profile `monitoring`) for error tracking.

x-api-env: &id001
  APP_ENV: ${APP_ENV:-staging}
  COOKIE_SECURE: ${COOKIE_SECURE:-false}
  ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY must be set}
  SECRET_KEY: ${SECRET_KEY:?SECRET_KEY must be set}
  SLUG_HMAC_KEY: ${SLUG_HMAC_KEY:?SLUG_HMAC_KEY must be set}
  DATABASE_URL: postgresql+asyncpg://${APP_DB_USER:-correlcore_app}:${APP_DB_PASSWORD:?APP_DB_PASSWORD must be set}@postgres:5432/${POSTGRES_DB:-correlcore}
  REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
  RATE_LIMIT_STORAGE_URL: redis://:${REDIS_PASSWORD}@redis:6379/1
  RATE_LIMIT_TRUST_PROXY_HEADERS: 'false'
  CORS_ORIGINS: ${CORS_ORIGINS}
  SMTP_HOST: ${SMTP_HOST:-mailpit}
  SMTP_PORT: ${SMTP_PORT:-1025}
  SMTP_USER: ${SMTP_USER:-}
  SMTP_PASSWORD: ${SMTP_PASSWORD:-}
  SMTP_FROM: ${SMTP_FROM:-noreply@correlcore.local}
  FRONTEND_BASE_URL: ${FRONTEND_BASE_URL:-http://127.0.0.1:${WEB_HOST_PORT:-3000}}
  UNVERIFIED_CLEANUP_DAYS: ${UNVERIFIED_CLEANUP_DAYS:-7}
  DEV_VIEW_ENABLED: ${DEV_VIEW_ENABLED:-false}
  IMAGE_TAG: ${IMAGE_TAG:-latest}
  IMAGE_DIGEST: ${IMAGE_DIGEST:-}
services:
  migrate:
    image: ${IMAGE_REGISTRY:-ghcr.io/sturmi77}/correlcore-api:${IMAGE_TAG:-latest}
    pull_policy: always
    container_name: correlcore-migrate
    command:
    - alembic
    - upgrade
    - head
    environment:
      APP_ENV: ${APP_ENV:-staging}
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?ENCRYPTION_KEY must be set}
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY must be set}
      SLUG_HMAC_KEY: ${SLUG_HMAC_KEY:?SLUG_HMAC_KEY must be set}
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-correlcore}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-correlcore}
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      RATE_LIMIT_STORAGE_URL: redis://:${REDIS_PASSWORD}@redis:6379/1
      RATE_LIMIT_TRUST_PROXY_HEADERS: 'false'
      CORS_ORIGINS: ${CORS_ORIGINS}
      SMTP_HOST: ${SMTP_HOST:-mailpit}
      SMTP_PORT: ${SMTP_PORT:-1025}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-noreply@correlcore.local}
      FRONTEND_BASE_URL: ${FRONTEND_BASE_URL:-http://127.0.0.1:${WEB_HOST_PORT:-3000}}
      UNVERIFIED_CLEANUP_DAYS: ${UNVERIFIED_CLEANUP_DAYS:-7}
      DEV_VIEW_ENABLED: ${DEV_VIEW_ENABLED:-false}
      IMAGE_TAG: ${IMAGE_TAG:-latest}
      IMAGE_DIGEST: ${IMAGE_DIGEST:-}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
    - correlcore
    restart: 'no'
    security_opt:
    - no-new-privileges:true
  api:
    image: ${IMAGE_REGISTRY:-ghcr.io/sturmi77}/correlcore-api:${IMAGE_TAG:-latest}
    pull_policy: always
    container_name: correlcore-api
    restart: unless-stopped
    environment: *id001
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    networks:
    - correlcore
    security_opt:
    - no-new-privileges:true
    healthcheck:
      test:
      - CMD-SHELL
      - python -c "import urllib.request; urllib.request.urlopen(\"http://localhost:8000/health/live\")" || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    ports:
    - ${TAILSCALE_IP:-127.0.0.1}:${API_HOST_PORT:-8210}:8000
  web:
    image: ${IMAGE_REGISTRY:-ghcr.io/sturmi77}/correlcore-web:${IMAGE_TAG:-latest}
    pull_policy: always
    container_name: correlcore-web
    restart: unless-stopped
    environment:
      NODE_ENV: production
      INTERNAL_API_URL: http://api:8000
    depends_on:
      api:
        condition: service_healthy
    ports:
    - ${TAILSCALE_IP:-127.0.0.1}:${WEB_HOST_PORT:-3000}:3000
    networks:
    - correlcore
    security_opt:
    - no-new-privileges:true
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO- http://localhost:3000 > /dev/null 2>&1 || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
  postgres:
    image: pgvector/pgvector:pg16
    container_name: correlcore-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-correlcore}
      POSTGRES_USER: ${POSTGRES_USER:-correlcore}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}
      APP_DB_USER: ${APP_DB_USER:-correlcore_app}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD:?APP_DB_PASSWORD must be set}
    volumes:
    - postgres_data:/var/lib/postgresql/data
    - ./initdb:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-correlcore} -d ${POSTGRES_DB:-correlcore}
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
    - correlcore
    security_opt:
    - no-new-privileges:true
  redis:
    image: redis:7-alpine
    container_name: correlcore-redis
    restart: unless-stopped
    command: 'redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes --appendfsync everysec

      '
    volumes:
    - redis_data:/data
    healthcheck:
      test:
      - CMD
      - redis-cli
      - -a
      - ${REDIS_PASSWORD}
      - ping
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
    - correlcore
    security_opt:
    - no-new-privileges:true
  mailpit:
    image: axllent/mailpit:latest
    container_name: correlcore-mailpit
    restart: unless-stopped
    environment:
      MP_MAX_MESSAGES: 5000
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1
    ports:
    - ${TAILSCALE_IP:-127.0.0.1}:8025:8025
    networks:
    - correlcore
    security_opt:
    - no-new-privileges:true
    healthcheck:
      test:
      - CMD
      - wget
      - -q
      - --spider
      - http://localhost:8025/api/v1/info
      interval: 30s
      timeout: 5s
      retries: 3
  worker:
    image: ${IMAGE_REGISTRY:-ghcr.io/sturmi77}/correlcore-api:${IMAGE_TAG:-latest}
    pull_policy: always
    container_name: correlcore-worker
    restart: unless-stopped
    command:
    - supercronic
    - -passthrough-logs
    - /app/crontab/worker.crontab
    environment: *id001
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    networks:
    - correlcore
    profiles:
    - worker
    security_opt:
    - no-new-privileges:true
volumes:
  postgres_data:
    name: correlcore_postgres_data
  redis_data:
    name: correlcore_redis_data
networks:
  correlcore:
    driver: bridge
    name: correlcore
