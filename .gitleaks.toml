[extend]
useDefault = true

[allowlist]
description = "CI unit-test Fernet fixture only (not a production secret)"
# Exact ENCRYPTION_KEY used in .github/workflows/ci-api.yml pytest env.
# Do NOT path-allowlist the whole workflow file — that would hide future leaks.
regexes = [
  '''SFt_zAqsk202KSZePwlHDy3TkhiHG1rFDBePciArND4=''',
]
