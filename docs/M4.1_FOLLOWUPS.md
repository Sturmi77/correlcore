# M4.1 Follow-up Work

Tracked from Codex PR #257 review (2026-06-30). P1 fixes landed in the review-fix commit;
M4.1.1 hardening (#258) implemented 2026-07-15.

## Backend

- [x] **Initial pull backfill** — hydrate `sync_revision_log` from existing entries/tags/symptoms created via online API before first offline sync (`ensure_revision_log_backfill`)
- [x] **Revision increment locking** — `SELECT FOR UPDATE` on `sync_user_revisions` to avoid duplicate `(user_id, user_rev)` under concurrent push
- [x] **Slot collision on client create** — merge by `(entry_date, slot)` when client UUID is unknown to server; IntegrityError nested-transaction retry
- [x] **Note conflict detection** — distinguish two non-empty notes when both reduce to `present: true` (`_note_conflict_markers`)
- [x] **Sync validation errors → 400** — catch Pydantic `ValidationError` in push path, map to `SyncBadRequestError`
- [x] **Custom tag/symptom `updated_at`** — set `updated_at=client_ts` on create so subsequent updates in same batch are not skipped
- [x] **Conflict retention vs RLS** — retention DELETE policy via `app.sync_retention` (migration 023)

## Frontend

- [x] **Tag/symptom pull application** — apply non-entry pull deltas into Dexie `tags` / `symptoms` tables; refresh stores
- [x] **Per-user Dexie partition** — `correlcore-offline-<userId>` (logout wipe retained)
- [x] **Multi-tab push serialization** — `navigator.locks` (`correlcore-sync-push`)

## QA

- [ ] **Two-browser simultaneous edit** — manual spot-check before beta
- [ ] **Multi-account same browser** — verify logout wipe + fresh client_id

GitHub: [#258 — M4.1.1 Offline sync hardening](https://github.com/Sturmi77/correlcore/issues/258)
