# M4.1 Follow-up Work

Tracked from Codex PR #257 review (2026-06-30). P1 fixes landed in the review-fix commit;
items below remain for M4.1.1 / beta hardening.

## Backend

- [ ] **Initial pull backfill** — hydrate `sync_revision_log` from existing entries/tags/symptoms created via online API before first offline sync
- [ ] **Revision increment locking** — `SELECT FOR UPDATE` on `sync_user_revisions` to avoid duplicate `(user_id, user_rev)` under concurrent push
- [ ] **Slot collision on client create** — merge by `(entry_date, slot)` when client UUID is unknown to server (avoid `uq_entries_user_date_slot` 500)
- [ ] **Note conflict detection** — distinguish two non-empty notes when both reduce to `present: true`
- [ ] **Sync validation errors → 400** — catch Pydantic `ValidationError` in push path, map to `SyncBadRequestError`
- [ ] **Custom tag/symptom `updated_at`** — set `updated_at=client_ts` on create so subsequent updates in same batch are not skipped
- [ ] **Conflict retention vs RLS** — worker cleanup must bypass owner-only DELETE policy (maintenance role or policy amend)

## Frontend

- [ ] **Tag/symptom pull application** — apply non-entry pull deltas locally or defer cursor until handled
- [ ] **Per-user Dexie partition** — optional `user_id` in DB name or tables (logout wipe is interim fix)
- [ ] **Multi-tab push serialization** — `BroadcastChannel` or leader election (ADR-0036 §5)

## QA

- [ ] **Two-browser simultaneous edit** — manual spot-check before beta
- [ ] **Multi-account same browser** — verify logout wipe + fresh client_id

GitHub: [#258 — M4.1.1 Offline sync hardening](https://github.com/Sturmi77/correlcore/issues/258)
