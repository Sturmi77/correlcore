# SMB/NAS-friendly pnpm layout for Windows network shares (UNC or mapped drives).
# Symlink-heavy isolated node_modules often fails on Synology SMB mounts.
# See docs/DEVELOPMENT.md — "Development on network shares (NAS/SMB)".
node-linker=hoisted
package-import-method=copy
